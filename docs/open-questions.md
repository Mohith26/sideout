# Open questions

Questions the build spec ([`build-spec.md`](./build-spec.md) §17) marks **OPEN**. None of
these are guessed at in code. Each one has a documented fallback that is implemented,
marked at the point of use with an `// OPEN:` comment, and listed here with the phase that
owns it. Later phases append to this file rather than resolving anything silently.

| # | Question | Fallback implemented | Owner phase | Code marker |
|---|---|---|---|---|
| 1 | **Sandbox credentials.** Keys and `tenantId` are issued by a Lucra representative; there is no self-serve path. | `LUCRA_MODE=mock` is the default and the only mode the app needs to boot: `src/lucra/mock.ts` is a faithful in-process Lucra (API key enforced, exact documented error bodies, the ported matcher, manual close for tournaments, `AUTOMATED` auto-settlement for recreational games, signed webhooks), rebuilt from the database at boot. Sandbox/production require `LUCRA_BASE_URL` and `LUCRA_BACKEND_API_KEY` and fail fast at boot without them. The Web SDK (`lucra-web-sdk`) is **not on the public npm registry** (`npm view` answers 404); the documented install is from GitHub (`Lucra-Sports/lucra-web-sdk`, latest tag v1.12.0). The pin lives in `src/lucra/version.ts` and is reported by `/health`; it is not in `package.json` (a GitHub dependency would make every `npm i` reach GitHub), and `src/lucra/version.test.ts` keeps the two in step if it is ever added. | 1 (env), 4 (mock, pin) | `src/env.ts`, `src/lucra/mock.ts`, `src/lucra/version.ts` |
| 2 | **Forge vs legacy REST.** The public reference is marked legacy and superseded by Forge (`https://forge.sandbox.lucrasports.com/docs/`, an interactive surface not published as pages). Which should a new integration target? | Legacy documented shapes (`Version 1.1`, last updated 2026-03-03). Every path, header and error string is in `src/lucra/endpoints.ts`; every request and response schema in `src/lucra/types.ts`; nothing else names a Lucra path. Lucra states webhook payloads and types are unchanged in Forge. | 4 | `src/lucra/endpoints.ts`, `src/lucra/types.ts` |
| 3 | **Webhook signature scheme.** The spec says it is not published; it now is (`server-to-server/webhook-subscriptions/request-verification`, `legacy/2.0_webhook_setup`): `X-Lucra-Signature: sha256=<hex>`, an HMAC of the raw body under the per-webhook shared secret. The request-verification page still hedges ("Let's assume the hashing algorithm is HMAC-SHA256"). | Exactly that, in one swappable function (`verifyWebhookSignature`): HMAC-SHA256 over the raw bytes with `LUCRA_WEBHOOK_SECRET`, constant-time compare. Outside mock mode a missing secret warns at boot and on every delivery, and the receiver answers 401 rather than accepting anything unverified. In mock mode the mock signs with a built-in secret when none is configured. **Webhook event id** (new, same row): no published payload carries an event or delivery id, and the docs' own idempotency example keys on matchup id plus event type. `deriveEventId` builds that key for the known types (plus the joining user for `TournamentUserJoined`, the user for the user events) and a sha256 of the body for anything else; no field of the payload is taken as an id, so a per-delivery id Lucra might add later can never defeat the dedupe. An unverified delivery is logged and refused with 401 and never persisted; the route caps the body at 256 KB before reading it. | 4 | `src/lucra/webhook-signature.ts`, `src/server/lucra-webhooks.ts` (`deriveEventId`) |
| 4 | **Idempotency.** Does Lucra support a first-class idempotency key on score writes? | Our own key: a UUID v7 minted by `enterAgreed` in `src/server/consensus.ts` the first time a consensus reaches `agreed` (`idempotencyKeyFor` returns an existing key untouched, so a resolution after a dispute or any later replay never re-mints), unique on `match_consensus.idempotency_key`, and required by `assertMayWriteToLucra` before any Lucra write. Phase 4 reuses it for every attempt row in `lucra_score_submissions` and echoes it in `metadata`. | 3 (mint), 4 (send) | `src/domain/consensus.ts` (`idempotencyKeyFor`, `assertMayWriteToLucra`), `src/server/consensus.ts` (`enterAgreed`), `src/server/lucra.ts` (`submitConsensusScores`: every attempt row carries the key, unique on `(idempotency_key, attempt)`, and it rides in `metadata.idempotency_key`) |
| 5 | **GameId and LocationId registration** for an outdoor, non-fixed venue sport. How should `locationId` be modeled for a travelling event? | `tournaments.lucra_location_id` is nullable and left `null`; `lucra_game_id` is the single constant `SIDEOUT_BEACH_2V2`. | 1 (schema), 4 (adapter) | `src/db/schema.ts` (`tournaments.lucra_location_id`), `src/seed/build.ts` (`LUCRA_GAME_ID`, `tournament()`), `src/domain/lucra-score.ts` (`locationId` is omitted from the write while null; `gameId` is always sent) |
| 6 | **Closing tournaments via API.** The legacy reference documents `POST /api/rest/pool-tournament/:matchupId/complete` (a `paymentStructure` naming winners, `unassignedUserIds` as a warning) and says completion "triggers payout distribution"; whether partners may call it, versus the console being authoritative, is still not stated. | The organizer's close is the trigger (`lucraSettlementHook` → `settleTournament`): the documented complete call is sent with the frozen preview's rewards split per player (Lucra's tie shape: consecutive positions, `positionOverride` = the team's placement). Success moves the tournament to `settled`; a refusal leaves it `awaiting_settlement` with a blocking `LucraAlert` and a "Settle again" action on `/admin/lucra`. A `TournamentCompleted` webhook never settles or awards anything: it confirms a tournament the organizer already settled and raises a blocking alert on any other (a close from Lucra's console is reconciled by the organizer, not honoured automatically). | 4 | `src/server/lucra.ts` (`settleTournament`, `buildPaymentStructure`), `src/server/close.ts`, `src/server/lucra-webhooks.ts` |
| 7 | **State coverage.** Lucra's own surfaces say 42, 43, and 44 states for different products. Which applies to a free-to-play tournament, and what is the runtime source of truth? | Never hardcoded in copy: `LUCRA_STATE_COVERAGE` is `null` and copy says "where Lucra is available" until the authoritative source is known. Nothing in the docs read for phase 4 (the REST reference, the SDK pages) publishes the number. | 4 (constant), 5 (copy audit) | `src/lucra/version.ts` (`LUCRA_STATE_COVERAGE`) |
| 8 | **Charitable gaming interaction.** Does a charitable-gaming regime apply on top of the skill-contest framework when entry fees are donations and prizes are sponsor-funded rewards? | No copy anywhere implies legal clearance. Donation and prize ledgers are separate tables with no foreign key between them. | 1 (schema), 5 (copy audit) | `src/db/schema.ts` (`donations`) |
| 9 | **Score attestation roadmap.** Is there appetite for a partner-side attestation or dual-confirmation contract of the kind implemented in §10? | Built and enforced on our side regardless: both teams submit from their own phone, each scoreline is canonicalized to the match orientation and hashed (`canonicalizeSubmission`), agreement is hash equality between submissions from two different teams (`judgeSubmission` compares team ids, resolved from `team_members`), a disagreement is `disputed` until an organizer resolves it with an attributed scoreline, and only an `agreed` consensus can reach Lucra (`assertMayWriteToLucra`). Every submission row and transition is kept (`score_submissions` is never updated, only superseded; `audit_log` records each transition), so a future attestation contract would have the full trail to sign over. Nothing in the Lucra write depends on Lucra acknowledging any of it. | 3 (done) | `src/domain/consensus.ts`, `src/server/consensus.ts`, `src/domain/scoreline-hash.ts` (`canonicalizeScoreline`) |

## Phase 1 additions

- **Pinned Lucra SDK version.** `/health` reports the pin from `src/lucra/version.ts`
  (`lucra-web-sdk` 1.12.0, GitHub-only; see row 1). Phase 4b initializes that version in
  the browser.

## Phase 4 additions

| Question | Fallback implemented | Owner phase | Code marker |
|---|---|---|---|
| **A finished attempt and later writes.** The docs say a score "will continue to overwrite the prior score ... unless `attemptFinished=true` was passed, the tournament is closed ... or the user has paid for another attempt", and that `attemptFinished: true` "prevents further score updates for this attempt" — but not whether such a write is reported in `failedMatchupIds` or silently kept. Sideout writes each player once per match with `attemptFinished: true`, and a player plays several matches in one Lucra matchup. | The mock records a write to a finished attempt as affected and the user as `locked` in its ingestion log, leaving the score unchanged (the documented overwrite sentence, read literally); a write to a CLOSED tournament lands in `failedMatchupIds`. Under that reading only a player's first match score stands on Lucra's leaderboard, which is fine because prizes are assigned explicitly at close (`paymentStructure` from the frozen preview) and never from Lucra's ranking. If Lucra reports locked writes as failures instead, every later match would classify as `partial`; the row and the `/admin/lucra` view would show it immediately. Whether one Lucra matchup per Sideout **match** is the intended mapping (the spec's `externalId` format `sideout-{slug}-{match_id_short}` hints at it, while its data model — `tournaments.lucra_matchup_id`, one close per tournament — says per tournament) is the question to put to Lucra. | 4 | `src/lucra/mock.ts` (`applyScore`), `src/domain/lucra-score.ts`, `src/server/lucra.ts` (`buildPaymentStructure`) |
| **`score` for a doubles match.** Lucra's tournament leaderboard takes one number per user; the spec's example (`"score": 21` with `sets: "21-18,19-21,15-12"`) does not say which number. | The points the player's team won across the agreed sets, with the full scoreline from that team's side, the match id, the team id, `won`, the round and the idempotency key in `metadata`. | 4 | `src/domain/lucra-score.ts` (`buildLucraScoreWrite`) |
| **Web SDK sign-in payload.** `POST /api/me/lucra/link` records the Lucra user id "when supplied by the SDK sign-in payload" (phase 4b); the SDK's `client.user` shape is not read here. | The route only mints `external_id` and refuses any other field: the Lucra user id is the payout destination and the key `UserKYCVerified` matches on, so it is never taken from the caller. The server learns it from Lucra alone — the participant read-back (`syncLucraUserIds`, run before every settlement) and the `TournamentUserJoined` / `UserSignedUp` webhooks, each keyed on the `externalId` Lucra echoes (an id already claimed by another link is never moved; unique index on `lucra_links.lucra_user_id`). Phase 4b's SDK sign-in should therefore end in a server-side read-back, not a client-supplied id. | 4 (server), 4b (client) | `src/server/lucra.ts` (`linkLucraAccount`, `syncLucraUserIds`) |

## Phase 2 additions

| Question | Fallback implemented | Owner phase | Code marker |
|---|---|---|---|
| **SMS delivery provider.** The brief leaves Sideout's own sign-in unspecified and names no provider for delivering one-time codes or partner invites. | Codes and invites go through one `SmsSender` seam. Outside production the only implementation writes the message through `src/lib/log.ts` (the phone number masked, the code in clear so a developer can complete the flow) and the code is additionally returned by `POST /api/auth/request-code` as `devCode`. In production the log sender is not permitted — a live code in the hosting log would let anyone with log access take over the account — so `getSmsSender()` returns nothing: `POST /api/auth/request-code` answers `503 unavailable` with `detail.code = "sms_unavailable"` and issues no code, and the partner-invite text is skipped (the invite still waits under the partner's `GET /api/me`). A real provider implements `SmsSender` and is returned from `getSmsSender()`; callers do not change. | 2 (seam), later (provider) | `src/server/auth/sms.ts` |

## Follow-ups (not OPEN, just not built yet)

| Item | Status | Where |
|---|---|---|
| **Double elimination.** `double_elim` is in the `tournaments.format` enum (§6.2) but the phase-2 draw engine does not build it: `draw()` refuses it with `DrawError("unsupported_format")` and the route answers `409 conflict` with `detail.code = "unsupported_format"`. The bracket module (`advanceWinner`, `next_match_id`/`next_match_slot`) is shaped so a losers' bracket can hang off the same links. | Not built; refused honestly | `src/domain/draw.ts` (`draw`), `docs/build-spec.md` §6.2 |
| **Reopening registration.** The status machine is exactly the chain in the brief (`draft → registration_open → registration_closed → live → awaiting_settlement → settled`, `cancelled` from any pre-live state). There is no `registration_closed → registration_open` edge; add one in `src/domain/transitions.ts` if organizers need it. | Not built | `src/domain/transitions.ts` |
| **Closing and settlement edges.** `live → awaiting_settlement` is the close flow (`POST /api/admin/tournaments/:id/close` after `GET …/close/preview`, blocked while any match is unresolved or any consensus is `disputed`/`submitting`/`rejected`/`partial`, refused on a stale preview hash). `awaiting_settlement → settled` is the settlement outcome and belongs to phase 4 (`lucraSettlementHook` in `src/server/close.ts` is the seam; it reports `not_available` today). `PATCH /api/admin/tournaments/:id` still refuses both edges, and the builder's status section offers only the edges it takes plus a link to the close flow while `live`. | Close done (3); settled is phase 4 | `src/server/close.ts`, `src/server/tournaments.ts` (`isPatchableTarget`), `src/components/organizer/StatusActions.tsx` |
| **Bracket seeding trigger.** For `pool_to_bracket`, round 1 is seeded from pool standings by `seedBracketFromPools` (`POST …/draw` with `{ stage: "bracket" }`), which applies the advancement rule stored on the tournament. `seedBracketIfPoolsComplete` calls it whenever a pool match becomes terminal — agreed, resolved by an organizer, or forfeited — and it was the last one, with round 1 still unseeded, after the resolving transaction committed (a failure is logged and reported as `bracketSeeded: false`, never undoing the result; the organizer's request stays available). | Wired in phase 3 | `src/server/matches.ts` (`seedBracketIfPoolsComplete`), `src/server/consensus.ts`, `src/server/draw.ts` |
| **Starting a match.** `scheduled → in_progress` is in the match machine (players, organizers or the system may take it). No route takes it on its own: the first legal score submission walks a `scheduled` match through `in_progress` to `awaiting_scores` as the submitting player, setting `started_at` and auditing each step, so play is recordable without an organizer. The organizer live board (`/organizer/events/[id]/board`) derives each court's current match from status and schedule (`getCourtBoard`) and has no explicit "on court now" action; add one if organizers need to open a match before a score arrives. | Player submission opens it | `src/domain/transitions.ts`, `src/server/consensus.ts` (`openForScores`), `src/db/queries/board.ts` |
| **Forfeit on a disputed match.** An organizer may still forfeit a disputed match; the match becomes `forfeited`, leaves the dispute queue and no longer blocks the close, while its `match_consensus` row stays `disputed` (the enum has no state for "set aside") and records `resolved_by_user_id` plus `disputed_reason` "Settled by forfeit" in the same transaction, with a `consensus.settled_by_forfeit` audit row. The match page shows the two readings as history, and a further submission is refused as `match_not_open`: every reader keys on the match status. Nothing will ever be written to Lucra for it (`assertMayWriteToLucra` refuses `disputed`). A dedicated consensus state for this is a candidate if phase 4 needs to list it. | Documented behaviour | `src/server/matches.ts` (`forfeitMatch`), `src/db/queries/consensus.ts` (`listDisputes`), `src/server/close.ts` (`blockerReason`), `src/app/m/[id]/page.tsx` |
| **Projected rewards.** The close preview freezes `rewards` rows with status `projected`, but no route creates them yet; an organizer projects rewards by inserting rows (the seeded settled event has `awarded`/`claimed` rows only). The event builder (`/organizer/events/[id]`) edits sponsors and their prize contributions but has no rewards section; it should own this. | Not built | `src/server/close.ts` (`previewClose`), `src/components/organizer/EventForm.tsx` |
| **Match links from cards.** Every match on screen links to `/m/[id]`: `LiveMatchStrip` and the Overview's "On the sand now" cards wrap `MatchCard` in the link (the card itself carries none), every bracket node and pool-sheet cell on the Bracket tab links through its `href`, and the court board links each row. The Standings tab ranks teams, not matches. | Done (2b) | `src/components/tournament/LiveMatchStrip.tsx`, `src/components/bracket/model.ts` (`nodesFromMatches`), `src/components/bracket/PoolSheets.tsx` (`poolMatchRefs`) |
| **Trusted proxy hops.** `POST /api/auth/request-code` and `/verify` key a per-address rate limit on `x-forwarded-for` only when `TRUSTED_PROXY_HOPS` says how many proxies vouch for it; a Next.js route handler has no socket address of its own, so with the default `0` only the per-phone and process-wide (`AUTH_CODE_GLOBAL_CAP`, default 2000 per ten minutes, charged only for a code actually issued) limits apply and production warns at boot. The public deployment sits behind at least one proxy and must set `1` (or the depth of its chain). | Deployment setting | `src/env.ts`, `src/server/auth/rate-limit.ts` |
| **Cross-pool ranking.** The spec defines within-pool tiebreaks (§11.2) but not how pool winners (or runners-up, or the best remaining) from pools of unequal size compare for bracket seeds. Implemented per match played: win percentage, set ratio, point differential per match, points for per match, team id (`CROSS_POOL_ORDER`). | Implemented; adjust if the organizer wants another convention | `src/domain/standings.ts` (`compareAcrossPools`) |
| **Responsible gaming link.** §4.7 asks for Lucra's responsible gaming policy wherever a wallet or reward is shown, but names no URL. The profile links `LUCRA_RESPONSIBLE_GAMING_URL` (default: Lucra's published policy page) once; the self-limit tooling lives on that page and in Lucra's SDK. The wallet chip and verification row (§11.5) are not rendered until the SDK exists; `/me` carries a documented slot for them. | Phase 4 confirms the URL and mounts the chip | `src/env.ts`, `src/app/me/page.tsx` |
| **Public page cache headers.** The brief asks for short public-page caching. Pages are `force-dynamic` (they read the session cookie for draft previews and the viewer's own-team highlight), so Next answers `private, no-cache`; the standings API keeps `max-age=10` and the live tabs refresh on that cadence client-side. A CDN policy for anonymous `/t/[slug]/*` needs a `Vary: Cookie`-aware layer in front of the app. | Deployment setting | `src/components/ui/LiveRefresh.tsx`, `src/app/api/tournaments/[slug]/standings/route.ts` |
| **Console navigation for Lucra.** The console nav has Events and Disputes; `/admin/lucra` (§11.6) exists and gates itself but is not yet in the nav. | Add the link (phase 5 polish) | `src/app/organizer/layout.tsx`, `src/app/admin/lucra/page.tsx` |
| **WebKit run of the e2e suite.** Playwright's `mobile` and `desktop` projects both run on Chromium (one browser download). Below md the console's events list is one card per event with the stretched link on a positioned `<article>`, the pattern `TournamentCard` uses, because WebKit does not treat a positioned `<tr>` as a containing block; until a `webkit` project joins `playwright.config.ts`, `/organizer/events` at 390px is checked in Safari by hand. | Manual check | `playwright.config.ts`, `src/app/organizer/events/page.tsx`, `e2e/interactions.spec.ts` |
| **Redirects below a streamed layout.** `/t/[slug]/register` redirects anonymous visitors to sign-in from inside the `/t/[slug]` layout, so the response is a 200 carrying Next's client redirect rather than a 307; browsers land on `/sign-in` either way. `/me`, `/teams/new` and `/organizer/**` redirect or 404 before streaming. | Cosmetic | `src/app/t/[slug]/register/page.tsx` |
| **Lucra tournament entry at registration.** Joining a Lucra tournament is the player's own SDK action (auto-join on sign-in, or `api.joinTournament`), so the server cannot enrol anyone: `lucraEntryHook` now answers `awaiting_sdk_join` with each roster member's link state and whether the matchup is verified, for the registration screen's join step (phase 4b). The organizer's reconciliation (`GET /api/admin/tournaments/:id/lucra/participants`) is the truth about who joined; auto-join is never relied on (§7.5). | Server half done (4); client join step is 4b | `src/server/registration.ts`, `src/server/lucra.ts` (`reconcileParticipants`) |
| **Recovering a tournament frozen by rule 7.3.4.** When the pre-write query returns a count other than one, the spec's rule moves a live tournament to `awaiting_settlement` with a blocking alert and no frozen close preview. Two exits: the organizer's "Verify targeting" takes the `awaiting_settlement → live` edge (organizer-only, audited) once the query returns exactly one matchup, and play resumes with the queued `agreed` consensus written on its next write; or the organizer closes from the frozen state (`closeTournament` runs preview → confirm without a second status edge) and settlement's sweep writes what was queued. A query that never answered (transport, 5xx, shape) has no count and does not freeze: the consensus stays `agreed` under a non-blocking `matchup_query_failed` alert until the settlement sweep or a verify. Organizers should still run "Verify targeting" before an event goes live. | Built | `src/server/lucra.ts` (`ensureMatchupTarget`), `src/server/close.ts` (`isFrozenByLucra`), `src/domain/transitions.ts` |
| **A write the process never finished.** The attempt row and `agreed → submitting` commit before the network call (§8.1), so a restart mid-call leaves the consensus `submitting` with a `pending` row, which no gate admits. `sweepStaleSubmissions` (once per process on the Lucra layer's first use, and before every settlement sweep) turns a `pending` row older than `STALE_SUBMISSION_MS` (2 minutes, above the client's worst case) into `transport_error` and the consensus into `rejected` (audit `lucra.stale_attempt_swept`), so the organizer's retry applies. | Built | `src/server/lucra.ts` (`sweepStaleSubmissions`) |
| **Consensus-time write latency.** The Lucra write runs inside the score and resolve routes after the consensus commits, so the phone waits for it (instant in mock mode; bounded by the client's 5s/10s timeouts and three retries per player call, run in parallel). Moving it behind Next's `after()` would return sooner but lose the deterministic response the sheet and the tests rely on. | Inline for now | `src/app/api/matches/[id]/scores/route.ts`, `src/server/lucra.ts` (`writeAgreedConsensus`) |
| **Lucra client-side flows.** `LucraGate`, SDK initialization (`lucra-web-sdk` 1.12.0 from GitHub), the identity, wallet and demographic flows, the registration entry step, the profile's Lucra rows, the wallet chip and the rewards sheet. | Phase 4b | `src/lucra/version.ts` |

## Spec deviations

Places where the spec is followed in intent but not to the letter, each with the reason
the spec's "deviate only with a stated reason" rule asks for.

| Where | Spec says | Implemented | Reason |
|---|---|---|---|
| `--text-tertiary` in `src/styles/tokens.css` | `#646C79` (§12.1) | `#7C8491` | The spec value measures 3.76:1 on `--bg-base` and 3.29:1 on `--bg-overlay`, below the 4.5:1 that §12.6 and acceptance #21 require for every text tier. Lightened to the nearest value in the same hue that clears 4.5:1 on all four surfaces (4.62:1 on `--bg-overlay`); `src/styles/tokens.test.ts` asserts it. |
| `teams.status` in `src/db/schema.ts` | `enum(registered, checked_in, withdrawn)` (§6.1) | adds `forming` first and `disbanded` last | `POST /api/teams` (captain plus a pending partner invite) happens before `POST /api/tournaments/:slug/register` (both members in, donation intent created), and the spec's enum has no state for a team in between. `forming` teams never count toward capacity and are not listed publicly. A forming team never traps its players: a member who creates another team (`POST /api/teams`, e.g. after a mistyped number or the wrong partner accepting) or accepts an invite to one (`POST /api/teams/:id/join`) disbands it in the same transaction — its pending invite `revoked`, its status `disbanded`, the audit row naming every member released — so crossed invites resolve as soon as one side joins. `disbanded` is hidden from the public roster like `forming`; `withdrawn` stays reserved for a team that had registered and is listed as before. Only a registered team refuses a second one. Migrations `0001` and `0003` rebuild the CHECK constraint. |
| `users.role` in `src/db/schema.ts` | not in §6.1 | `enum(player, organizer)`, default `player` | The organizer routes are "role-gated" (§9) and the spec's `users` table has no role. Lucra still owns wallet identity; this is only Sideout's thin account. Seeded organizers do not play. |
| `team_invites`, `auth_codes` tables | not in §6 | added in migration `0001` | Partner invites by phone (§11.4) and one-time sign-in codes need a home; neither is identity data (§4.6): an invite holds a phone, a code row holds only an HMAC. |
| `POST /api/admin/matches/:id/forfeit` | not in §9 | added | Confirmed by the task's build instructions, which asked for an organizer forfeit path that the organizer console (phase 2b) drives. It is the only route that resolves a match in phase 2 and it never sets `final`. |
| `GET /api/admin/tournaments/:id/close/preview` | §9 lists only `POST …/close` | added | §11.6 requires the close to be an explicit two-step confirm over a frozen preview; the preview needs its own read so the organizer can review it (and its hash) before `POST …/close` requires that hash back. |
| `score_submissions.submitted_for_team_id` | not null (§6.3 lists it as a plain column) | nullable (migration `0004`) | An organizer's resolution is recorded as a submission row so the trail is complete (§6.3: "one row per submission attempt"), and it speaks for neither team; `resolved_by_user_id` on `match_consensus` and the `audit_log` entry attribute it. |
| `tournaments.close_preview_json` | not in §6.2 | added (migration `0004`) | §11.6 freezes the standings and projected payouts before anyone commits; the frozen preview and its hash are stored on the row so the close page and any later settlement show exactly what was confirmed. |
| `POST /api/auth/request-code`, `POST /api/auth/verify`, `POST /api/auth/logout`, `POST /api/dev/login` | not in §9 | added | The spec leaves Sideout's own session unspecified. Phone + one-time code is the minimum that fits the invite-by-phone flow; the dev login exists only outside production or with `SIDEOUT_DEV_LOGIN=true` (a page-extension gate, asserted by `npm run test:bundle`). |
| Seed users | 48 (§13) | 48 players + 2 organizers | Organizer accounts are needed to exercise `/api/admin/*`; they hold no Lucra link and no team, so the 48-player, every-verification-state requirement is untouched. |
| Ephemeral session secret in `src/env.ts` | "random per-process secret" when `SESSION_SECRET` is unset in production | one random value per process, kept on `globalThis` | A production server evaluates the env module once per route bundle; a secret minted per module instance meant a cookie issued by the sign-in route failed to verify on the next page. The value is still random and still dies with the process. |
| `Container` in `src/components/shell/Container.tsx` | §12.5 lists `AppShell` | split out of `AppShell` | `AppShell` reads the session to show the console tab, which drags `server-only` code into any client component (`error.tsx`) that imported the content column from the same file. |
| `POST /api/rest/pool-tournament/user-score` body | `userScores: [...]` (§7.2 example) | a single `userScore` object, one call per player | The documentation says so in as many words ("Unlike the tournament score ingestion endpoint which accepts a single user score, this endpoint accepts an array"); the generic `/user-score` and the recreational endpoint take the array, and the adapter sends it there. The attempt row stores every per-player call. |
| `tournaments.lucra_external_id` format | `sideout-{tournament_slug}-{match_id_short}` (§7.3.3) | `sideout-{slug}-{tournament id short}` (phase 1, kept) | The column is per tournament and the spec's data model has one matchup per tournament (`lucra_matchup_id`, one close); a match id in a per-tournament key does not exist. See the phase 4 OPEN row on attempts. |
| `awaiting_settlement → live` | not in §6.2 | added, organizer only | The exit from a rule 7.3.4 freeze (above); a closed tournament, one with a frozen preview, never takes it. `lucra_webhook` remains an audit actor only: no status edge is taken by a webhook. |
| `tournaments.lucra_matchup_verified_at`, `tournaments.lucra_alert_json` | not in §6.2 | added (migration `0005`) | The §7.3.4 assertion result is cached per tournament (the matchup id alone does not say it was verified), and the "blocking organizer alert" the spec requires needs a home the console and `/admin/lucra` can read. |
| `lucra_links.lucra_user_id` | no constraint (§6.1) | unique index (migration `0005`) | The verification state that arrives by webhook keys on the Lucra user id; a second Sideout account claiming the same id would inherit another player's compliance state. |
| Organizer Lucra routes | §9 lists `GET /api/admin/lucra/submissions` | plus `POST …/matches/:id/lucra/retry`, `POST …/tournaments/:id/lucra/verify`, `POST …/tournaments/:id/lucra/settle`, `GET …/tournaments/:id/lucra/participants` | The §10 `organizer_retry` edge, the §7.3.4 assertion, a refused settlement, and the §7.5 participant reconciliation each need an organizer action; `/admin/lucra` drives them. |
